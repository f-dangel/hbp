from setuptools import setup

setup(
    author="Anonymous Authors",
    name="bpexts",
    version="0.2",
    description="Hessian backpropagation in PyTorch",
    install_requires=[],
    license="MIT",
    packages=["bpexts"],
    zip_safe=False,
    python_requires=">=3.5",
)
